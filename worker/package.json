{
  "name": "relieo-media-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Videur d'acces aux medias Relieo (Cloudflare Worker + binding R2).",
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "selftest": "node scripts/selftest.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.103.0"
  }
}
